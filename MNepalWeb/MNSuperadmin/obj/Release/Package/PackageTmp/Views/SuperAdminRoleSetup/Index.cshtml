
@{
    ViewBag.Title = "Role Setup Modify SuperAdmin Role";
}

@model List<MNSuperadmin.Models.UserProfilesInfo>
<!-- =============================================== -->
<!-- Content Header (Page header) -->
<section class="content-header">
    <h1>

         Role Setup
        @*<small>Modify Staff/Admin Role</small>*@
    </h1>
    <ol class="breadcrumb">
        <li><a href="#"><i class="fa fa-user"></i> Role Setup</a></li>
        <li class="active"> Modify SuperAdmin Role</li>
    </ol>
</section>

<!-- Main content -->
<section class="content">
    <!-- Main row -->
    <div class="row">

        <!-- Start Form -->
        <div class="col-md-12">
            <div class="box box-primary">
                <div class="box-header with-border">
                    <h3 class="box-title">Modify SuperAdmin Role</h3>
                </div>

                <!--Message-->
                @if (this.ViewData["admin_messsage"] != null)
                {
                    if (this.ViewData["message_class"].ToString() == "success_info")
                    {
                        <div class="alert alert-success">
                            <button type="button" class="close" data-dismiss="alert">&times;</button>
                            <h4>Success</h4> @this.ViewData["admin_messsage"]
                        </div>
                    }
                    else
                    {
                        <div class="alert alert-danger">
                            <button type="button" class="close" data-dismiss="alert">&times;</button>
                            <h4>Error</h4> @this.ViewData["admin_messsage"]
                        </div>
                    }
                }
                <!-- /.box-header -->
                <div class="box-body">

                    <div class="row">
                        <div class="col-md-10">

                            <table id="t01" class="table table-bordered table-striped">
                                <thead>
                                    <tr>
                                        <th>Name</th>
                                        <th>Description</th>
                                        <th>Status</th>
                                        <th colspan="3">Action</th>
                                    </tr>
                                </thead>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>
                                            @item.ProfileName
                                        </td>
                                        <td>
                                            @item.ProfileDesc
                                        </td>
                                        <td>
                                            @if (item.UserProfileStatus == "Active")
                                            {
                                                <span class="badge bg-green-active"> @item.UserProfileStatus </span>
                                            }
                                            else
                                            {
                                                <span class="badge bg-red-active"> @item.UserProfileStatus </span>
                                            }
                                        </td>
                                        <td style="width: 10px;">
                                            <a href="@Url.Action("EditAdminProfileSetup", "SuperAdminRoleSetup", new {profileCode = @item.UPID})"><i class="fa fa-pencil fa-fw" title="Edit"></i></a>
                                        </td>
                                        <td style="width: 10px;">
                                            <a href="@Url.Action("ViewAdminProfileSetup", "SuperAdminRoleSetup", new {profileCode = @item.UPID})"><i class="fa fa-eye" title="View"></i></a>
                                        </td>
                                    </tr>

                                }

                            </table>

                        </div>
                    </div>

                </div>
            </div>
        </div>
        <!--End Form -->

    </div>
    <!-- /.row -->
</section>
<!-- /.content -->
<!-- page script -->
@section scripts{
    <script>
        $(function () {
            $('#t01').DataTable();
        });
    </script>
}
<!-- =============================================== -->
